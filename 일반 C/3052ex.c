#include <stdio.h>

int main(void) {
    int result = 0;
    int rest[42] = {};
    for (int x;x<42;x++) {
        rest[x] = x;
    }
    for (int k=0;k<10;k++) {
        int a; 
        scanf("%d", &a);
        for (int)
    }
}